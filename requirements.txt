# Complete runtime stack. Keep this file aligned with pyproject.toml.

numpy==1.26.4
torch==2.2.2
gymnasium==1.3.0
pettingzoo==1.26.1
python-dotenv==1.2.2

# SUMO Python bindings — keep aligned with installed SUMO version (1.27.0).
sumolib==1.27.0
traci==1.27.0

# Plotting for dissertation figures (train convergence, ablation bars).
matplotlib==3.11.0
