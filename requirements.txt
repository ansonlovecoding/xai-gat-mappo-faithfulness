# Core env stack — pinned for reproducibility.
# RL/GNN deps (torch, torch-geometric, etc.) will live in a separate file.

numpy==2.4.6
gymnasium==1.3.0
pettingzoo==1.26.1
python-dotenv==1.2.2

# SUMO Python bindings — keep aligned with installed SUMO version (1.27.0).
sumolib==1.27.0
traci==1.27.0
